import { TotalMessagePipe } from './total-message.pipe';

describe('TotalMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
